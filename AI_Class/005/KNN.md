# KNN (K-Nearest Neighbors)

최근접 이웃이라고 부른다.


새로운 관측값이 주어지면 기존 데이터 중에서 가장 속성이 비슷한 K개의 이웃을 먼저 찾는다.

가까운 이웃들이 갖고 있는 목표 값과 같은 값으로 분류하여 에측한다.

K의 값에 따라 예측의 정확도가 달라지므로 적절한 K 값을 찾는 것이 매우 중요하다.



새로운 관측 point에 대해 기존 데이터와의 점과 점 사이의 거리를 통해서 최근접 데이터를 찾을 수 있다.